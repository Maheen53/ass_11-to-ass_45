"use strict";
//No Users: Add an if test to Exercise 28 to make sure the list of users is not empty.
//  If the list is empty, print the message Wef need to find some users!
// • Remove all of the usernames from your array, and make sure the correct message is print
let userName = ["eda", "sanem", "laila", "derine"];
if (userName.length > 0) {
    for (let myName of userName) {
        console.log("useername" + myName);
    }
}
else {
    console.log("we need to find some users");
}
let nullUser = [];
if (userName.length = 0) {
    console.log("username" + nullUser);
}
else {
    console.log("we need to find some users");
}
